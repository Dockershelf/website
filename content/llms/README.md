# llms-full corpus sections

Markdown in this folder feeds `/llms-full.txt` and per-route markdown twins via `lib/markdown/`.

When Home, Overview, Install, Community, or Contact copy changes in React components, update the matching `.md` file here so the AI corpus and twins stay aligned.
